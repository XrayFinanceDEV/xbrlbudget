// Gruppo APERTURA (v4 pagine 1-2: copertina, sintesi). Nessun componente
// proprio in questa fase: entrambe le pagine si rendono con il dispatch
// generico di `comuni.typ` (la copertina resta un caso speciale gestito
// direttamente da `editorial.typ`, perché usa `cover()` di `base.typ`, non
// un elemento di `page.items`). Un futuro bisogno specifico del gruppo
// (per esempio il blocco «Decisioni e punti da presidiare» della v4 pagina
// 2, oggi assente perché il modello v2 non porta ancora quei dati — vedi la
// ricevuta di fondazione) si scrive qui, non in `comuni.typ`.

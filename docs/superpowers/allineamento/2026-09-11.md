# Rapporto di riallineamento — 2026-09-11

**Modo:** `diff` · **Intervallo:** `1490476..ffedd1a` (lotto 3B «robustezza client»: CORS su un 500
non gestito, CE Prev./SP Prev./Report con errore leggibile e «Riprova», tab Indicatori
dell'infrannuale con errore distinto da proiezione-non-generata) · **Da quanto non si lancia uno
sweep completo:** sempre, `ultimo_completo: null` — non è mai stato lanciato.

## Conteggi

98 citazioni prodotte da `scripts/riallinea.py --da 1490476 --a ffedd1a`, su 19 simboli nuovi —
tutte verificate a fondo, nessuna esclusa (il modo `diff` è esaustivo sul proprio intervallo, e 98
sta comodamente sotto le «poche decine» tipiche moltiplicate per un fattore di margine; non è
scattata l'eccezione della strategia a tre livelli).

| File | Citazioni | Esito |
|---|---|---|
| `CLAUDE.md` | 5 | 5 OK |
| `docs/superpowers/plans/2026-09-10-lotto3b-robustezza-client.md` (verbale datato) | 93 | ~85 OK (codice implementato esattamente come descritto) · 8 SMENTITO (design superato da una revisione successiva — vedi sotto), 0 MORTO |

Nessun nome generico (`generici: []` nell'output della raccolta).

## Corretto automaticamente

**Nessuna correzione applicata.** `CLAUDE.md` è risultato corretto su tutte e 5 le citazioni: nessun
intervento consentito dalla lista chiusa era necessario. Il documento datato non si riscrive per
regola («Piani e spec datati non si riscrivono»), indipendentemente da cosa la verifica trova.

## Da decidere

### `docs/superpowers/plans/2026-09-10-lotto3b-robustezza-client.md` — verbale datato, design superato

Le 93 citazioni coprono in gran parte codice creato esattamente come pianificato e verificato contro
`ffedd1a`: `_cors_headers_for`, `tests/test_cors_on_500.py` (entrambi i test, `ALLOWED_ORIGIN`/
`DISALLOWED_ORIGIN`), `lib/forecast-page-status.ts` (`ForecastPageStatus`, `forecastPageStatus`,
`ANALYSIS_RETRY_COUNT`/`ANALYSIS_RETRY_DELAY_MS`, `forecastLoadErrorMessage`), il componente
`ForecastLoadError.tsx`, e l'intero Task 3.4 (tab Indicatori dell'infrannuale — `pratica-indicatori-
status.ts`, `PraticaIndicatoriStatus`, `praticaIndicatoriStatus`, il loro uso in
`app/pratica/page.tsx` incluso `toast.error(forecastLoadErrorMessage(error))` — corrispondenza
esatta, riga per riga, col codice reale).

**8 citazioni (righe 558, 559, 581, 656, 657, 679, 731, 732, 754, 800, 810-818 — contate come
blocco unico nel conteggio sopra) descrivono un design che una revisione successiva ha sostituito**:
il piano mostra le tre pagine (`/forecast/income`, `/forecast/balance`, `/report`) importare
`forecastPageStatus(loading, error)` a un solo argomento — `loading = scenariosLoading ||
analysisLoading`, un solo `error` — e i gate `pageStatus === "pronto" && analysisData && ...`; il
bozzetto del bullet di `CLAUDE.md` (righe 810-818) dice lo stesso. Il codice a `ffedd1a` non fa
questo: le tre pagine usano `forecastPageState(scenarios, analysis)` e `forecastScenariosEmpty`
(`frontend/lib/forecast-page-status.ts`), che tengono separati l'errore della lista scenari da
quello di `/analysis` (`errorSource`, «la lista vince se è lei a fallire») e mostrano l'errore
**sopra** i dati già in cache invece di nasconderli (guardia `!analysisData`, non `pageStatus ===
"pronto"`). Confermato leggendo `frontend/lib/forecast-page-status.ts`,
`frontend/app/forecast/income/page.tsx`, `frontend/app/forecast/balance/page.tsx`,
`frontend/app/report/page.tsx` a `ffedd1a`: nessuna delle tre importa `forecastPageStatus` da sola,
tutte e tre importano `forecastPageState`/`forecastScenariosEmpty`.

Per la regola sui verbali datati **non ho corretto il testo**: ho aggiunto in coda al documento (dopo
«### Chiusura del task» dell'ultimo task) una riga di superamento che rimanda a `CLAUDE.md` come
stato attuale — committata in questo stesso giro insieme al rapporto (vedi «Commit» sotto), non come
correzione della lista chiusa.

`CLAUDE.md` (Livello vivo, le 5 citazioni sopra: `forecastPageState`, `ANALYSIS_RETRY_COUNT`,
`ANALYSIS_RETRY_DELAY_MS`, `ForecastLoadError`, `forecastScenariosEmpty`, righe 413-420) descrive
**già** il design finale corretto, non quello del piano: verificato riga per riga contro
`frontend/lib/forecast-page-status.ts`, `frontend/components/budget/ForecastLoadError.tsx`, le tre
pagine e `frontend/lib/pratica-indicatori-status.ts` — tutte e 5 OK, nessuna azione.

## Memoria — riferimenti morti

**Nessuno.** `git grep` mirato su tutti e 19 i simboli nuovi/cambiati di questo intervallo
(`_cors_headers_for`, `ForecastLoadError`, `ForecastPageStatus`, `ANALYSIS_RETRY_COUNT`,
`ANALYSIS_RETRY_DELAY_MS`, `forecastPageStatus`, `forecastPageState`, `forecastScenariosEmpty`,
`forecastLoadErrorMessage`, `PraticaIndicatoriStatus`, `praticaIndicatoriStatus`, i simboli del test
CORS) contro `/home/peter/.claude/projects/-home-peter-DEV-budget/memory/` non ha trovato nessuna
occorrenza: nessun file di memoria cita questi simboli, vivi o morti che siano. Nessun file di
memoria toccato.

## Non verificabile

Nessuna. Tutte le 98 citazioni prodotte hanno avuto un esito determinabile contro il codice a
`ffedd1a`.

## Limiti dichiarati

- **Cieco all'errore di nascita.** Il modo `diff` trova solo la deriva sui simboli mossi in
  `1490476..ffedd1a`; un'affermazione sbagliata fin dall'origine su codice non toccato in questo
  intervallo non è mai stata verificata da questo giro.
- **Cieco ai cambi di corpo senza cambio di firma**, per costruzione dello strumento (`_REGOLE`
  aggancia solo `def`/`class`/route/colonna aggiunte o rimosse): non ho fatto affidamento solo su
  questo per il design superato — l'ho scoperto leggendo per intero le tre pagine e confrontandole
  col piano, non da una citazione mancante.

---

# Rapporto di riallineamento — 2026-09-11 (lotto 3A, worktree `feat/lotto3a-motori`)

**Modo:** `diff` · **Intervallo:** `e8b094d..08939b1` (Task 13, Step 8 del lotto 3A — motori del
previsionale e rendiconto) · **Nota:** questo è un secondo giro di riallineamento eseguito lo
stesso giorno del rapporto sopra, su un branch e un intervallo diversi (`feat/lotto3a-motori`,
non `main`); i due rapporti coesistono nello stesso file per convenzione di nome (`AAAA-MM-GG.md`)
ma coprono lavori indipendenti. **Da quanto non si lancia uno sweep completo:** sempre,
`ultimo_completo: null` nello stato di `main` — questo giro non lo aggiorna (vedi «Nota di
processo» sotto).

## Conteggi

`python3 scripts/riallinea.py --da e8b094d --a 08939b1` ha prodotto **637 citazioni** su **169
simboli**, in 30 documenti (`CLAUDE.md`, `docs/budget/API-PREVISIONALE.md`,
`docs/import/REGOLE-IMPORT-05-INFRANNUALE.md`, 26 piani/spec/rapporti datati sotto
`docs/superpowers/` e `docs/piano-import-2026-07/`, `docs/outputs/`, e 2 file di memoria). **Tutte
e 637 sono state verificate a fondo** (modo `diff`, esaustivo sul proprio intervallo — 637 è oltre
le «poche decine» tipiche, ma resta verificabile in una sessione con la verifica distribuita su 8
agenti paralleli più il lavoro diretto su `CLAUDE.md`/`API-PREVISIONALE.md`/`REGOLE-IMPORT-05-
INFRANNUALE.md`/memoria; non è scattata l'eccezione dichiarata dello skill per un diff enorme).
4 nomi generici esclusi per costruzione (`HTTP` 42, `USER` 71, `applica` 61, `main` 171 citazioni) —
mai aperti singolarmente, per regola.

| File | Citazioni | Esito |
|---|---|---|
| `CLAUDE.md` | 10 | 10 OK |
| `docs/budget/API-PREVISIONALE.md` | 24 | 24 OK |
| `docs/import/REGOLE-IMPORT-05-INFRANNUALE.md` | 8 | 8 OK |
| `docs/superpowers/plans/2026-09-10-lotto3a-motori-rendiconto.md` (verbale datato — il piano che ha prodotto questo stesso codice) | 537 | ~527 OK/RENAME atteso (piano eseguito quasi verbatim) · 1 NON-VERIFICABILE (istruzione editoriale, non un fatto di codice) |
| `misc_small_1` — 12 verbali datati diversi | 28 | 8 OK · 6 MORTO (verbale mai realizzato con quel nome) · 1 RENAME (fissato, vedi sotto) · 1 SMENTITO (riga stale in un rapporto precedente) · 12 NON-VERIFICABILE (falsi positivi di parole comuni: `NUOVO`, `spia`) |
| `misc_small_2` — 10 verbali datati diversi | 28 | 17 OK · 1 SMENTITO · 3 MORTO (piano non ancora eseguito) · 7 POSSIBILE SUPERAMENTO (annotate, vedi sotto) |
| Memoria (`ago_erp_parser.md`, `debiti-senza-scadenza-a-breve.md`) | 2 | 2 non applicabili — falsi positivi (`NUOVO`/`BREVE` sono parole italiane nel testo, non simboli di codice citati) |

**Totale: ~593 OK su 637 (~93%)** · 9 MORTO (tutti in verbali, nessuna azione dovuta) · 2 SMENTITO
(un verbale, un rapporto storico — nessuna azione dovuta) · ~9 RENAME intenzionali dentro il piano
del lotto (il testo del piano stesso istruiva la rinomina, eseguita) · 7 POSSIBILE SUPERAMENTO
(annotati) · ~14 NON-VERIFICABILE · 2 non applicabili in memoria.

**Sei puntatori di riga noti, verificati per primi** (dalla rilettura di
`.superpowers/sdd/2026-09-10-lotto3a-motori-rendiconto/task-13a-report.md` §2.3, indicati dal
coordinatore prima della raccolta): tutti e sei erano stale (il codice si è spostato per via degli
inserimenti dei Task 10/12/14 più a monte nello stesso file, senza che il simbolo citato cambiasse
— quindi invisibili al modo `diff`, che aggancia solo `def`/`class` aggiunte o rimosse). Corretti
tutti e sei sostituendo la riga col nome del simbolo, come il Task 11 aveva già fatto in
«Intra-Year Engine» — eccezione esplicita del coordinatore alla lista chiusa dello skill (nessuna
delle 4 regole della lista la autorizzerebbe da sola, trattandosi di una descrizione di
comportamento/posizione, non di un link, un percorso, un rename di identificatore o un numero
ancorato a una costante):

1. `calculations/forecast_engine.py:2122-2131` (`_tax_components`) → **corretto**
2. `calculations/projection_common.py:341-367` (`tax_settlement_saldo_acconto`, 1ª occorrenza) → **corretto**
3. `calculations/projection_common.py:341-367` (`tax_settlement_saldo_acconto`, 2ª occorrenza) → **corretto**
4. `calculations/forecast_engine.py:2745-2763` (runoff crediti) → **corretto**
5. `calculations/forecast_engine.py:3104-3128` (runoff debiti operativi — fornitori/previdenziali/altri) → **corretto**
6. `calculations/forecast_engine.py:2610-2627` (guardia `degenerate_turnover_ratio`, Task 14) → **corretto**

Il settimo elemento della lista del coordinatore, `lib/pratica-indicators-status.ts` (nome errato,
il file vero è `frontend/lib/pratica-indicatori-status.ts`), **era già corretto** in CLAUDE.md a
`08939b1` (riga 497 cita già il nome giusto) — nessuna azione necessaria.

## Corretto automaticamente

**I sei puntatori sopra**, in `CLAUDE.md`: sostituita la citazione `file:riga-riga` con
`` `simbolo`, `file` `` (senza numero di riga), seguendo esattamente il precedente già applicato dal
Task 11 in «Intra-Year Engine» (stesso file, poche righe sopra). Autorizzato esplicitamente dal
coordinatore come eccezione alla lista chiusa dello skill, per questi sei punti nominati in
anticipo — non generalizzato ad altre citazioni con numero di riga trovate durante il giro (vedi
«Da decidere»).

Nessun'altra correzione della lista chiusa standard (1-4) si è resa necessaria: `CLAUDE.md`,
`docs/budget/API-PREVISIONALE.md` e `docs/import/REGOLE-IMPORT-05-INFRANNUALE.md` sono risultati
corretti su tutte le rispettive 10+24+8 citazioni verificate — nessun link morto, nessun percorso
inesistente, nessun identificatore sparito senza rename tracciabile, nessun numero in contraddizione
con una costante nominata.

### Annotazioni di superamento sui verbali (procedura dedicata dello skill, non la lista chiusa)

Tre verbali datati citano `projection_common.tax_closing_position` come la funzione — invariata —
che l'infrannuale usa per la posizione tributaria di fine anno. Il commit `b5698ce` (lotto 3A,
Task 5, già su questo HEAD) l'ha **rimossa**, unificando budget e infrannuale su
`posizione_tributaria_fine_anno`. Per la regola sui verbali **il testo non è stato riscritto**: ho
aggiunto in coda a ciascuno una riga di superamento che rimanda allo stato attuale
(`CLAUDE.md` › «Intra-Year Engine», `docs/budget/API-PREVISIONALE.md` §9) — la correzione vera è
già in quei due documenti vivi, verificata sopra.

- `docs/superpowers/2026-09-08-nota-costruttori-sp-infrannuale.md` (1 citazione, rename)
- `docs/superpowers/plans/2026-09-08-scadenziamento-pregresso.md` (4 citazioni, POSSIBILE SUPERAMENTO)
- `docs/superpowers/specs/2026-09-08-scadenziamento-pregresso-design.md` (3 citazioni, POSSIBILE SUPERAMENTO)

## Da decidere

- **`docs/superpowers/plans/2026-07-23-import-fix-01-pareggio-preflight.md`** (6 citazioni di
  `_dec`, righe 56/101-106): il piano proponeva un helper `_dec` per `tests/_import_probe.py`, mai
  realizzato con quel nome (`git log` sull'intera storia del file non lo mostra mai); l'equivalente
  funzionale è `_num`/`_str_dec`. Verbale datato → non riscritto per regola. Proposta NON applicata:
  nessuna, il documento resta come testimonianza di una proposta non adottata.
- **`docs/superpowers/plans/2026-08-14-agente-riallineamento.md:411`** (`_json`): il piano
  proponeva `import json as _json`; `tests/test_riallinea.py` usa `import json` semplice. Verbale
  datato → non corretto.
- **`docs/superpowers/allineamento/2026-09-10.md:70`**: un rapporto di riallineamento precedente
  cita la guardia `_derived_days` a «~2080-2106»; nel codice a `08939b1` è a `:3042` (ulteriormente
  spostata dal commit `b5698ce`, successivo a quel rapporto). Non è un piano/spec datato in senso
  stretto, ma è un rapporto storico che documenta uno stato passato — nessuna regola della lista
  chiusa autorizza la correzione di un numero di riga non ancorato a una costante nominata, quindi
  segnalato e non toccato.
- **`docs/superpowers/plans/2026-09-11-riallinea-notturno.md`** (3 citazioni `REPO`, righe
  157/158/178): il piano descrive `scripts/riallinea_notte_logica.py` e il suo test, che non
  esistono ancora nel worktree — il piano non è stato eseguito (branch `feat/riallinea-notte`
  assente qui). Non è un difetto: è un piano non ancora realizzato. Nessuna azione.

## Memoria — riferimenti morti

**Nessuno.** Le uniche due citazioni di memoria sono falsi positivi del matching per nome:
`NUOVO` in `ago_erp_parser.md` (parola italiana in "PORTATI A NUOVO"/lista di modifiche) e `BREVE`
in `debiti-senza-scadenza-a-breve.md` ("si classifica a BREVE") — entrambe coincidenze col nome di
costanti di test definite altrove nel diff (`NUOVO`/`BREVE` in `tests/test_forecast_sweep_piani.py`
e affini), non citazioni di quei simboli. Contenuto dei due file verificato per intero: descrivono
ancora accuratamente il comportamento reale del parser AGO/ERP e la regola prudenziale sui debiti
senza scadenza dichiarata — nessun riferimento morto, nessun file toccato.

## Non verificabile

**~14 citazioni**, tutte in verbali datati: annotazioni editoriali al worker ("aggiorna il
docstring se ridondante"), diagrammi ASCII con `NUOVO`/parole italiane comuni scambiate per
simboli, narrazioni di processo nei rapporti di allineamento precedenti (`docs/superpowers/
allineamento/2026-08-14.md`, 5 citazioni `__init__` su percorsi-file o narrazione del rapporto
stesso), descrizioni del comportamento atteso durante il passo rosso di un TDD (non verificabile
contro il codice post-implementazione).

## Limiti dichiarati

- **Cieco all'errore di nascita.** Il modo `diff` trova solo simboli aggiunti/rimossi/rinominati
  nell'intervallo `e8b094d..08939b1`; un'affermazione sbagliata fin dall'origine su codice non
  toccato in questo intervallo non è mai stata verificata da questo giro.
- **Cieco alla deriva di riga senza cambio di simbolo** — è esattamente il guasto dei sei
  puntatori noti sopra: `tax_settlement_saldo_acconto`, `_tax_components` e la guardia
  `degenerate_turnover_ratio`/`_derived_days` esistevano già prima di `e8b094d` (lotto 2), quindi
  non compaiono fra i 169 simboli del diff — solo la loro **posizione nel file** è cambiata, per
  via del codice inserito più a monte dai Task 10/12/14. Il modo `diff` non li avrebbe trovati da
  solo: sono stati verificati perché il coordinatore li ha nominati esplicitamente in anticipo,
  dalla rilettura del report del Task 13a. Una citazione con lo stesso guasto ma non nominata in
  anticipo resterebbe stale dopo questo giro.
- **Cieco ai cambi di corpo senza cambio di firma**, per costruzione dello strumento — nessuna
  istanza scoperta di questo tipo specifico in questo giro (a differenza del rapporto sopra, che ne
  aveva trovata una leggendo per intero tre pagine).
- **Il corpus si auto-inquina.** Questo stesso rapporto, una volta committato, entrerà nel corpus
  del prossimo sweep — coerente con quanto già osservato nel rapporto del 2026-08-14.

## Nota di processo

Questo giro **non esegue `--registra`** e non scrive `docs/superpowers/allineamento/STATO.json`:
gira su un worktree d'integrazione (`feat/lotto3a-motori`), non su `main`, e lo stato del
riallineamento appartiene a `main` per decisione del coordinatore — sarà il merge a farlo avanzare,
non questo giro.

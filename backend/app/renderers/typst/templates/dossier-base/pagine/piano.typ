// Gruppo PIANO E RISULTATI (v4 pagine 7-10: ipotesi, CE previsionale, SP
// previsionale, flussi). Solo «ce» è implementata in questa fase
// (`dossier_catalog/piano.py`), e si rende interamente con il dispatch
// generico di `comuni.typ` (KPI + grafico + tabella di sintesi). «ipotesi»,
// «sp», «flussi» restano da fare: un bisogno di resa che quelle pagine
// avranno e che `comuni.typ` non copre ancora si scrive qui.

# Riallineamento 2026-09-19 — modo `diff`

- **Intervallo:** `b9ca1a6..23f05a5cc39668fb8ad438144047416ded06af50`
- **SHA verificato:** `23f05a5cc39668fb8ad438144047416ded06af50` (passato esplicito a `--a`, mai `HEAD` in corso d'opera)
- **Candidate:** 34 affermazioni (16 citazioni per simbolo + 18 citazioni per percorso di file) · **Verificate a fondo:** 34, più 15 affermazioni di regola raggiunte **dai commit** per argomento
- **Sweep completo mai lanciato:** `ultimo_completo` è `null` — questo è il primo giro dalla ripresa di `ripresa_l3`, quindi la copertura del Livello 3 non è mai avanzata
- **Simboli mossi:** 39 · **`MORTO`:** nessuno (`working_capital_mode` e `documenti_che_nominano` risultano entrambi vivi: il rimosso+aggiunto negli elenchi è la riga del commento / la firma, non la scomparsa)
- **Manopole nuove senza citazioni:** 0
- **Corrette da citazione: 0 · corrette da un commit: 2 · corrette da scansione meccanica dei link: 0** (una applicata e poi ritirata, vedi «Corretto automaticamente») — e **5 affermazioni false trovate tutte e solo dai commit**, zero dalle due chiavi di join. È il secondo giro consecutivo su questa linea, e il primo in cui il join non ne pesca **nemmeno una** (il giro scorso era 1 su 14). Le due correzioni sono **fuori lista chiusa, su incarico del proprietario**: il brief del collaudatore (`4dbfa05`) e il puntatore di `aliquota_service.py:10` (`280349f`, unico caso in lista chiusa *dopo* che il bersaglio è stato verificato).
- **Il rilievo 5 è stato corretto, e non per effetto di una regola di questo strumento:** è fuori lista chiusa (è una descrizione di comportamento) e l'autorizzazione è venuta dal proprietario, non dalla procedura. Vedi «Applicato su incarico». Insieme ad esso è stato **allargato il corpus**, che è la ragione per cui quel rilievo non era mai stato trovato: `RADICI_DOC` ora comprende `.claude/agents` e `.claude/skills`.

> **Misura che va detta prima di ogni altra cosa:** dei cinque rilievi sotto, **nessuno** era
> raggiungibile né da `citazioni` né da `citazioni_file`. `M2-02E-indicatori.md` non nomina alcun
> simbolo mosso e nessun file toccato; `PRATICA-PERCORSO.md:427` nomina `app/pratica/page.tsx`, che
> nell'intervallo **non è stato toccato**; `API-PREVISIONALE.md:901` idem. Le due chiavi hanno
> prodotto 34 righe, tutte vere. La spina dorsale ha funzionato proprio perché i `commits` sono la
> prima cosa che si è letta, non un controllo accessorio.

## Regole cambiate (dai commit)

Tre commit nell'intervallo toccano codice. Due cambiano una regola.

### `e894896` — fix(budget): la pagina non dice più che il motore usa l'aliquota effettiva

Il corpo nomina la regola (**`_tax_components` applica `tax_rate` così com'è; l'effettiva è solo la
proposta del wizard**) e nomina il simbolo. Cerco le pagine per **argomento**, non per simbolo:

| Pagina che enuncia la regola dell'aliquota | Esito |
|---|---|
| `CLAUDE.md:144`, `:334` | **OK** — «`tax_rate` is what runs, as written», l'effettiva è solo la proposta |
| `docs/budget/FORECASTING_GUIDE.md:392`, `:431-441` | **OK** — riscritto dal giro scorso sulle tre righe di `StepImposte` |
| `docs/budget/API-PREVISIONALE.md:360-365` | **OK** |
| `docs/import/REGOLE-IMPORT-05-INFRANNUALE.md:124` | **OK** — «i due motori ora fanno la stessa cosa» |
| `docs/testing/M2-02E-indicatori.md:30-45` | **OK** — ha già la «Nota 2026-09-18» che dichiara morta la motivazione |
| `docs/testing/M2-02E-indicatori.md:99-101` | ⚠ **SMENTITO** — vedi «Da decidere» §1 |
| `docs/frontend/PRATICA-PERCORSO.md:427` | ⚠ **SMENTITO** — vedi «Da decidere» §2 |
| `.claude/agents/collaudatore.md:156-160` | ⚠ **SMENTITO e fuori corpus** — vedi «Da decidere» §3 |

Prova nel codice: `calculations/forecast_engine.py:2841` `_tax_components` — `rate =
Decimal(str(assumption.tax_rate)) / Decimal('100')`, nessun `aliquota_effettiva`, nessun cap 60%,
`base_inc` parametro mai letto. La cap 60% vive ora in `projection_common.aliquota_effettiva:365`,
la cui docstring dice da sola: *«E' la regola che il motore applicava da solo fino al 2026-09-18:
ora serve a PROPORRE l'aliquota (e alla migrazione), mai a sostituirla»*.

### `649a554` — docs(allineamento): correzioni dimostrabili

È un commit di riallineamento, ma tocca anche `calculations/intra_year_engine.py`,
`calculations/projection_common.py`, `database/models.py`: **diff verificato, solo commenti e
docstring**, nessuna riga di comportamento. Le sue sette voci le riverifico comunque per argomento,
perché il corpo le elenca:

| Voce del corpo | Esito |
|---|---|
| §1 aliquota | **parzialmente applicata** — 4 pagine a posto, 2 rimaste false (sopra) |
| §2 `working_capital_mode` | **OK, verificato a fondo** — tre valori in `backend/app/schemas/budget.py:354,495`, `NULL`→`storico` letto in `intra_year_engine.py:648`, `_PASSI_EQUILIBRIO = 12` (`:685`) contro «bisects it for 12 steps» di `CLAUDE.md:757`, `_MIN_TURNOVER_OBSERVATION_MONTHS = 3` (`:197`), `MODI_CIRCOLANTE` in `frontend/lib/pratica-circolante.ts` = le tre medesime. `REGOLE-IMPORT-05` §5-bis confermato |
| §3 credito tributario d'apertura | **parzialmente applicata** — `FORECASTING_GUIDE:420` e `REGOLE-IMPORT-05:137-148` a posto; `API-PREVISIONALE:901` rimasto falso (sotto) |
| §4 TFR quota statutaria / anno consolidato | **OK** — `REGOLE-IMPORT-05:93`, `:216`, `CLAUDE.md:404`, `API-PREVISIONALE:1131-1146` |
| §5 «62 rotte» → via il conteggio | **OK** — `CLAUDE.md:74` dice «about 75» e il conteggio reale è **75** (`@router.*` in `backend/app/api/v1/*.py`) |
| §7 `kind` del catalogo | **OK** — `"panel"` nasce in `dossier_catalog/shared.py:559` (`panel_grid`) e si srotola in `__init__.py:86,112`; `"dumbbell"` in `indicatori.py:448,450` ed è in `CHART_KINDS` (`shared.py:28`). `M2-02D:44` li descrive esattamente così |
| §8 riferimenti (`sp07_growth` → `receivables_long_growth_pct`) | **OK** — `sp07_growth` non compare più in tutto il corpus; il nome reale è colonna in `database/models.py:623` |
| «`cash_out` dichiarato inaffidabile» | ⚠ **contatore scaduto** — vedi «Da decidere» §4 |

### `23f05a5` — feat(riallinea): i commit sono la spina dorsale

Strumento, non prodotto: tocca `scripts/riallinea.py` e i suoi test. Nessuna pagina di
documentazione enuncia una regola d'utente su come lavora la raccolta — l'unica è lo skill stesso
(`.claude/skills/riallinea/SKILL.md`), che è **fuori dal corpus** per costruzione e che va lasciato
coerente a mano. Verificate le due affermazioni che lo skill fa sul codice: `ESCLUSI_DAL_CORPUS`
(`scripts/riallinea.py:211`) e `RADICI_DOC = ["docs", "CLAUDE.md"]` (`:388`): **entrambe vere**.

## Citazioni verificate (tutte, 34/34)

Nessuna producesse un rilievo, ma va detto quali ho aperto:

- **`working_capital_mode`** (3 citazioni: `CLAUDE.md:751`, `REGOLE-IMPORT-05:387,390`) → **OK**,
  verifica a fondo nella tabella sopra.
- **`documenti_che_nominano`** (12 citazioni, tutte in
  `docs/superpowers/plans/2026-08-14-agente-riallineamento.md`) → il simbolo **vive** in
  `scripts/riallinea.py:270` con firma `documenti_che_nominano(simboli, radici)`; il piano a `:267`
  scrive `radici: list[str]`, un'annotazione in più. È un **verbale** e la differenza è il tipo
  dichiarato, non il comportamento: **non si tocca**.
- **`test_riallinea.py`** (18 citazioni `citazioni_file`, tutte nello stesso piano) → il file esiste,
  216 righe aggiunte nell'intervallo. **OK.**
- **`working_capital_mode`** in `specs/2026-08-14-agente-riallineamento-design.md:212` → citazione
  *dentro* l'annotazione che lo stesso `23f05a5` ha aggiunto: **OK**, è la prova, non l'affermazione.

## Generici (riportati, non aperti per costruzione)

`Commit` (185 citazioni), `git` (740). Per i percorsi: `page.tsx` (493), `intra_year_engine.py`
(124), `projection_common.py` (68), `models.py` (58), `riallinea.py` (50). Nessuno entra nella
verifica per nome.

## Corretto automaticamente

Nessuna, a bilancio. L'unica correzione del giro è stata **applicata, committata in `0ac033d`, poi
ritirata** — quindi il log contiene un commit `docs(allineamento): correzioni dimostrabili` che
dimostrabilmente non dimostra nulla, e un secondo commit che lo annulla.

| File | Che cosa | Esito |
|---|---|---|
| `docs/superpowers/plans/2026-08-14-claude-md-snellito.md:43` | link `[design](specs/2026-08-14-claude-md-snellito-design.md)` portato a `(../specs/...)`, perché risolto da `docs/superpowers/plans/` non puntava a nulla | **ritirata 2026-09-19**: la riga sta dentro un fenced block che è il *modello* del file da creare in `docs/superpowers/`, un livello più in su, dove `specs/…` risolve — e il file reale, `docs/superpowers/2026-08-14-inventario-claude-md.md:3`, scrive proprio `specs/…` e punta a un file che esiste. Il link non era morto: era il mio punto di partenza a essere sbagliato. |

Da imparare: la stessa scansione aveva **giustamente** lasciato stare `:99` perché «dentro un
fenced block»; a `:43` ha applicato la correzione sulla premessa opposta, sullo stesso file e nello
stesso giro. Un link dentro un blocco di codice si risolve dal percorso del file che quel blocco
**genera**, non da quello del file che lo contiene — e prima di toccarlo si apre il file generato.

**Come l'ho trovato: non lo ha trovato il tools.** La scansione dei link markdown è uno script
temporaneo che ho scritto in questo giro. `riallinea.py` **non ha un controllo di validità dei
link**, e la lista chiusa §§1-2 esiste da sempre: va bene che a eseguire la correzione sia il
modello, ma oggi tocca a lui *scoprire* i candidati a mano, giro dopo giro. Vedi «Da decidere» §5.

## Da decidere

### 1. `docs/testing/M2-02E-indicatori.md:99-101` — la pagina si smentisce da sola, sessanta righe più in basso

> «Nota su `effective_tax_rate`: il valore resta stabile a ~57,76% da `closing:2026` in poi perché
> **il motore di previsione deriva l'aliquota effettiva una sola volta dall'anno base** (qui la
> chiusura promossa) **e la riapplica a ogni anno di piano** (`_tax_components`)»

**Il codice fa il contrario**, e la stessa pagina lo dice a `:37-41`: «`_tax_components` **non deriva
nessuna aliquota**: applica `tax_rate` così com'è». La Nota §2 è stata aggiunta dal giro scorso, la
nota qui sotto no: la pagina ora afferma in un punto che la regola è morta e, sessanta righe dopo,
la applica come viva. È il difetto che il corpo di `649a554` diceva di aver chiuso («il riquadro
"la formula è duplicata" che si smentiva tre righe dopo») — stessa classe, altro punto della stessa
pagina.

Proposta (NON applicata): riscrivere la causa della stabilità in `57,76%` come **`tax_rate`
salvato sulle righe di piano** — il valore è costante fra gli anni perché la stessa aliquota si
ripete, non perché fu derivata una volta. La tabella a `:91-96` non cambia: misura, non spiega.

### 2. `docs/frontend/PRATICA-PERCORSO.md:427` — «la costante 27,9» non è più ciò che il wizard invia

> «Il `tax_rate` inviato è la costante **27,9** (IRES + IRAP), non un'aliquota derivata dalle
> imposte modificate»

`frontend/app/pratica/page.tsx:856-857` (e `:957`, secondo percorso della stessa funzione):

```ts
// L'aliquota del consuntivo di riferimento, non il 27,9 fisso: e' la
// proposta del commercialista (2026-09-18), stessa regola del budget.
const aliquota = (await getAliquotaProposta(importResult.companyId, fiscalYear - 1)).aliquota;
```

e la passa come `tax_rate: aliquota` (`:873`, `:978`). **La costante 27,9 la manda solo la forma
Startup** (`frontend/app/budget/page.tsx:441 STARTUP_TAX_RATE_PCT = 27.9`). Il corpo di `649a554`
elencava esattamente questo rilievo fra le «Trovate applicando, fuori dal rapporto: "ogni chiamante
manda 27,9" (**solo la forma Startup, ora**)» — la correzione è arrivata a `FORECASTING_GUIDE` e
non qui.

Proposta (NON applicata): la prima frase diventa «il `tax_rate` inviato è l'**aliquota proposta**
del consuntivo di riferimento (`GET …/aliquota-proposta`), non il 27,9 fisso, che resta solo nella
forma Startup». La seconda metà del paragrafo regge ed è confermata: `ce20_override` vince davvero
(`forecast_engine.py:2855-2858`, interpretato come imposta **totale**, quindi `current_tax =
totale − differite`).

### 3. `.claude/agents/collaudatore.md:156-160` — **CORRETTO** (vedi «Applicato su incarico»), e il confine del corpus è stato chiuso

> «**L'aliquota inviata non è quella applicata.** Le schermate mandano 27,9% … ma il motore la usa
> **solo come ripiego**: se l'anno base ha un'aliquota effettiva utilizzabile … **applica quella**
> (`forecast_engine.py:564-573`)»

Doppia falsità: la regola è ribaltata da `073927b` (vedi prova sopra), e **`forecast_engine.py:564-573`
oggi è un commento sugli alias del debito bancario**, estraneo alle imposte.

**Esito: Applicato** — (a) la sezione è stata riscritta e (b) `RADICI_DOC` è stato allargato, entrambe
su incarico esplicito del proprietario, non per effetto della lista chiusa. Dettagli, prova e misura
dell'allargamento nelle due sezioni seguenti. Il rilievo non era «una frase sbagliata»: era **un
limite del corpus**, e come tale va letto.

### 4. `CLAUDE.md:717` — «two tests only» sono tre

> «that field has no reader in production (**two tests only**, same for the budget
> `TaxYear.cash_out`)»

I file che leggono `cash_out` sono **tre**: `tests/test_intra_year_imposte.py`,
`tests/test_projection_common_runoff.py`, `tests/test_tax_kernel_compensazione.py` (gli ultimi due
sulla stessa funzione, il terzo aggiunto dopo la frase). La parte sostanziale della frase resta
vera e verificata: `cash_out` non ha **alcun** lettore in produzione — `git grep` lo trova solo nei
dataclass (`projection_common.py:399`, `:438`), nelle loro espressioni e nei commenti.

Non in lista chiusa: è un conteggio di file, non un valore ancorato a una costante nominata.
Proposta: «three test files, no production reader» — o, meglio, togliere il numero come si è fatto
per le rotte a `CLAUDE.md:74`: un conteggio di file invecchia allo stesso modo.

### 5. La lista chiusa §§1-2 non ha un produttore di candidati

`riallinea.py` enumera `citazioni`, `citazioni_file`, `non_documentati`, `generici`. **Non enumera
link relativi morti né percorsi fra backtick inesistenti**, che sono le due uniche voci della lista
chiusa interamente meccaniche — le uniche correggibili senza aprire il codice. In questo giro
l'unico candidato che quella scansione ha prodotto era un **falso positivo** (correzione applicata e
poi ritirata, sopra), e l'ho trovato con uno script scritto a mano in questa sessione: un altro giro
senza di me non lo trova, e quando lo trova va comunque aperto il file che il blocco genera.

Misura della scansione manuale (per capire se vale un flag): **2 link relativi apparentemente morti** in tutto il
corpus — e a verifica fatta **nessuno dei due lo era**, perché stanno entrambi dentro un fenced block (l'altro è `2026-08-14-claude-md-snellito.md:99`, dentro un fenced block che mostra una tabella
di esempio — non è un link renderizzato, non l'ho toccato), e **174 percorsi fra backtick che non
risolvono**. La seconda cifra è quasi tutta falso positivo per disegno: nomi di scratchpad di
sessione, file di altri repo (`budget-iframe.tsx`, dichiarato tale), file dati (`content_list.json`),
e file **esistiti e cancellati** che la pagina ha già annotato (`editorial_inventory.py`,
`M2-02B:9,24,46,68` con la «Nota 2026-09-18» che ne dichiara la scomparsa — **OK, non un rilievo**).
Un `--link` varrebbe la pena solo per la prima classe; per la seconda servirebbe un blacklist di
percorsi-legittimi-che-non-sono-file.

## Applicato su incarico — `.claude/agents/collaudatore.md` (rilievo 5)

**Non l'avrebbe autorizzato nessuna regola di questa procedura.** È una descrizione di
comportamento: la lista chiusa la vieta, e il rapporto l'aveva messa correttamente in «Da decidere».
L'autorizzazione è venuta dal proprietario, con una ragione che la lista chiusa non sa esprimere:
quel file **non è documentazione da leggere, è il brief di un agente che emette verdetti** — una
regola falsa lì non confonde un umano, produce direttamente rilievi sbagliati a catena. Stessa
classe di costo di `CLAUDE.md`, che invece è nel corpus da sempre.

Verificato **sul codice prima di scrivere**, non sulla memoria delle bullet del file:

| Affermazione scritta | Prova |
|---|---|
| `tax_rate` è l'aliquota applicata così com'è | `ForecastEngine._tax_components` → `rate = Decimal(str(assumption.tax_rate)) / Decimal('100')`; nessuna chiamata ad `aliquota_effettiva`; `base_inc` parametro mai letto |
| l'effettiva si **propone** | `GET /companies/{company_id}/years/{year}/aliquota-proposta` (`backend/app/api/v1/financial_years.py:567`) → `aliquota_service.aliquota_proposta`, che ripiega su `ALIQUOTA_RIPIEGO` = 27,9 |
| il cap 60% non sta più nel motore | vive in `projection_common.aliquota_effettiva`, chiamata da `aliquota_service`; nella docstring: «regola che il motore applicava da solo fino al 2026-09-18 … ora serve a PROPORRE» |
| il 27,9 fisso lo manda solo la forma Startup | `STARTUP_TAX_RATE_PCT = 27.9` in `frontend/app/budget/page.tsx:441`; il percorso infrannuale chiama `getAliquotaProposta` (`app/pratica/page.tsx:858,959`) |
| `ce20_override` scavalca, ed è imposta **totale** | `_tax_components`: `total_tax = ce20_override`, `current_tax = max(0, totale − differite)` |
| anticipate fuori dal CE | `deferred_tax_position(None, …)` (la griglia non arriva più); `sp06f`/`sp07f` costanti; contropartita `sp12e`, mai la cassa |

Il riferimento in fondo è ora al **simbolo** (`ForecastEngine._tax_components`), non
all'intervallo di righe: `forecast_engine.py:564-573` è la prova che un intervallo invecchia — era
vero quando il bullet è stato scritto, oggi è un commento sugli alias del debito bancario.

## Il corpus era il difetto — `RADICI_DOC` allargato

`scripts/riallinea.py:388` era `["docs", "CLAUDE.md"]`. È il motivo per cui il rilievo 5 non è stato
trovato da **nessun** giro, in nessuno dei due modi, con nessuna delle due chiavi: non una verifica
mancata, un file invisibile. Corretto in `358263b`, in TDD: prima i due test rossi (50 passed, 2
failed — e fallivano per la ragione giusta: trovavano `docs/` e `CLAUDE.md`, non
`.claude/agents/`), poi le radici. Il secondo test fa `chdir` in una `tmp_path` e chiama
`documenti_che_nominano` **con le radici di produzione**, perché `.claude` inizia con un punto: una
scansione che saltasse le directory nascoste avrebbe fatto sparire il solo file interessante, senza
alcun errore.

**La misura, chiesta perché la SKILL nomina decine di simboli e si temevano citazioni fantasma:**

| | prima | dopo | delta |
|---|---|---|---|
| `--completo`, citazioni totali | 11 338 | **11 366** | **+28 = 0,24%** |
| di cui `.claude/agents/collaudatore.md` | 0 | 17 | — |
| di cui `.claude/skills/riallinea/SKILL.md` | 0 | 9 | — |
| `diff` su `b9ca1a6..23f05a5` | 16 | **18** | +2, entrambe **vere** |
| file `.md` nel corpus | 147 | **149** | +2 |
| manopole nuove non documentate (`--completo`) | 40 | 40 | 0 |

**Non ho aggiunto alcuna esclusione, e la ragione è la misura, non un giudizio.** Le 17 di
`collaudatore.md` sono affermazioni **sul** codice (`safe_divide`, `ALLOWED_ORIGINS`,
`_tax_components`, `ce20_override`, `sp06f_growth_pct`, `DSCR`, `fixed_materials_*`): esattamente la
classe che cercavamo, e la verifica di questo stesso giro ne ha già confermata una. Le 9 della SKILL
sono auto-descrittive (`SOGLIA_GENERICO` ×3, `ESCLUSI_DAL_CORPUS`, `salva_stato`, `CLI`): lavoro da
leggere, non da cancellare — e il fatto che la SKILL affermi «`RADICI_DOC` contiene docs e
CLAUDE.md» è diventato **verificabile per via**, che è come questo giro ha corretto sé stesso.

Il precedente di `superpowers/allineamento/` **non si trasferisce**: i rapporti sono *sottoprodotti*
della misura e rientrando gonfiano le soglie dei generici (`create` 38→45, misurato); le istruzioni
agli agenti sono *input*. Se la SKILL crescesse fino a distorcere i conteggi, la voce da escludere
è `.claude/skills/` — e **+28 / 0,24% è il parametro** con cui deciderlo, non un'impressione.

**Un artefatto reso visibile, preesistente e non toccato:** una riga che cita una rotta con
segnaposto produce **3 citazioni per 1 affermazione** (`{id}` vs `{company_id}` vs il prefisso),
`collaudatore.md:162`. È rumore di conteggio, non un falso positivo: nessuna delle tre cambia il
verdetto. Vale per ogni pagina che nomina una rotta parametrizzata, anche fuori dalle radici nuove.

## Applicato su incarico (2) — `aliquota_service.py:10`, e la lezione che vale per lo strumento

> «Il frontend porta la stessa regola in `lib/aliquota-proposta.ts`.»

Corretto in `280349f` → `frontend/lib/budget-tax-rate.ts`, bersaglio **unico e verificato**: la sua
intestazione scrive la regola per intero (`ce20_override` = imposta totale → poi `tax_rate` così
com'è, proposto dall'ultimo depositato con ripiego 27,9) e cita `_tax_components`. Fin qui, un
puntatore morto come tanti.

**Ma non è deriva: è errore di nascita**, e la prova sta nel piano. Task 6 di
`docs/superpowers/plans/2026-09-18-imposte-commercialista.md` elencava «Create
`frontend/lib/aliquota-proposta.ts`» a `:104`, e **dodici righe sopra** (`:98`) porta l'annotazione
«Eseguito con una deviazione: niente `lib/aliquota-proposta.ts`; la proposta arriva dalla rotta».
Il docstring è stato scritto contro l'intento del piano, non contro l'esecuzione: non ha mai
smesso di essere vero, **non lo è mai stato**.

> **Nota onesta su questa stessa pagina.** Lo sha citato qui era `c1b77d1`, ed è diventato
> `280349f` perché ho emendato quel commit due volte: il rapporto l'ha seguito solo al terzo giro
> (e lo stesso vale per `b44c80d` → `358263b`, corretto a parte).
> **Il punto non è che fosse cambiato: è che `c1b77d1` rispondeva comunque a `git cat-file -e`, ma
> non è raggiungibile da HEAD** — `git gc` lo cancellerà, e a quel punto la citazione sarà un
> riferimento morto in una pagina che parla di riferimenti morti. Un predicato di esistenza non è
> un predicato di raggiungibilità: il controllo giusto è `git merge-base --is-ancestor <sha> HEAD`.
> In un verbale lo sha nudo va bene; in una pagina viva conviene aggrapparlo a un simbolo, come fa
> `REGOLE-IMPORT-05:123` (cita il commit *e* il simbolo).
>
> E ho scritto quella raccomandazione affermando che «`CLAUDE.md` cita `073927b`», che è falso:
> `CLAUDE.md` non nomina nessuno sha (verificato: zero occorrenze di backtick esadecimali a 7
> cifre). A citar sono `REGOLE-IMPORT-05:123` e `M2-02E:38`. Tre errori procurati dentro una
> sezione sui puntatori che invecchiano, tutti e tre nello stesso modo: la frase era *plausibile*,
> non letta. La verifica non è «aprire il file», è non fidarsi della propria ultima frase.

La distinzione non è accademica, è il confine di questo strumento: un puntatore che invecchia (la
classe di `forecast_engine.py:564-573`) è raggiungibile da un `diff`, perché il file che punta si è
mosso; uno nato sbagliato **non è raggiungibile da nessun giro, in nessuno dei due modi**, perché
non c'è stato nessun movimento da joinare. «Trova la deriva, non l'errore di nascita» qui si tocca
con mano: e l'unico meccanismo che l'ha trovato è stato **«verifica prima di scrivere»**, che in
questo giro non è una regola della procedura — è successo perché il proprietario ha chiesto di
correggere il brief del collaudatore, e correggerlo obbligava ad aprire `_tax_components`.

Due conseguenze praticabili, non applicate perché fuori di questo giro:
- scrivere i percorsi **dalla root** (`frontend/lib/…`, non `lib/…`): la forma nuda non è
  joinabile dal `citazioni_file`, che matcha il basename ma nasce dal percorso *toccato*; qui
  comunque non avrebbe aiutato (il file non esisteva), ma è il lato gratis della stessa cosa;
- un `Create` di un piano che viene disatteso in esecuzione **non ha un meccanismo di rientro**:
  il verbale annota la deviazione (giusto, non si riscrive), ma nessuno guarda le *Other* dei
  `Files:` a caccia di file mai nati. È una classe di falso che né `diff` né `--completo` vedrà.

## Da portare al proprietario (non è lavoro di riallineamento) — `sp06f_growth_pct`

**Rilievo, con la prova.** Il campo è **reso da nessun componente** (zero occorrenze di
`sp06f_growth` in `frontend/components/**`) e il motore lo ignora deliberatamente
(`forecast_engine.py`, commento «anticipate COSTANTI»): su questo `CLAUDE.md` è corretto. Ma
`frontend/lib/budget-wizard-steps.ts:45` lo dichiara ancora proprietà di
`STEP_FIELDS["patrimoniale-piano"]`, mentre `DEAD_FIELDS` (`:69`) — la lista in cui finiscono i
campi dichiarati e da nessun passo scritti, con il suo perché per ciascuno — **non lo contiene**.

La prova che non è un caso isolato ma una dimenticanza: `budget-circolante-step.ts` ha **zero**
occorrenze di `sp06f_growth`, mentre il piano del 2026-09-18 (`:102-103`) elencava proprio quel
file tra i «Modify … (riga `sp06f_growth_pct`)». Il piano non dice *che cosa* fosse quella riga
(è una parentetica, non un verbo), ma l'esito si misura: **lì la riga non c'è più, in
`STEP_FIELDS` sì**. Le due copie della stessa verità si sono separate, ed è tutta qui il rilievo.

Perché il test non lo vede: `budget-wizard-steps.test.ts:23-31` («ogni campo esposto appartiene a
un solo passo e i campi morti a nessuno») asserisce **unicità** e `DEAD_FIELDS ∉ STEP_FIELDS`, mai
che una voce di `STEP_FIELDS` sia realmente resa. Quindi `sp06f_growth_pct` in «Patrimoniale
piano» passa, e passa per sempre: **manca l'asse `STEP_FIELDS ⊆ resi`**. Il precedente utile è
`working_capital_mode`, che in `DEAD_FIELDS` c'è (`:71-74`) con scritto «scelta dell'INFRANNUALE,
nessun passo del wizard la scrive» — esattamente quello che varrebbe qui.

Non applicato, e deliberatamente: spostare una voce di `STEP_FIELDS` è una modifica al **client**
con un test congelato da aggiornare (`ivcee-catalog-parity` e la suite dei wizard-steps leggono
quegli elenchi), cioè una decisione su che cosa la UI dichiara di possedere. **La porta il
proprietario.** Chi volesse chiudere il buco per via, piuttosto, chiuda il test: un
`STEP_FIELDS ⊆ campi resi` renderebbe questa classe di deriva automaticamente rossa, e questo
rilievo non sarebbe arrivato a me.

## Memoria — riferimenti morti

Root scansionata: `/home/peter/.claude/projects/-home-peter-DEV-budget/memory` (35 file). Quattro
nomi di file non risolvono **in questo repo**, e nessuno è un riferimento morto:

| Nome | Dove | Perché non è morto |
|---|---|---|
| `budget-iframe.tsx` | `staging-deployment-topology.md:20` | la frase stessa dice «che sta nel repo genitore di Formula Finance, **non in questo**» |
| `auth.json` | `pi-agent-wsl.md:53-54` | file di configurazione di pi, non del progetto |
| `rilievi-workflow.md` | `percorso-ipotesi-budget-lotti.md:125` | «scratchpad della sessione », dichiarato come tale |
| `backend-env-file-is-relative.md` | `MEMORY.md:29` | file sibling della memoria, **esiste** |

**Nessuna correzione, nessuna rimozione:** la memoria non si tocca (regola 6).

## Non verificabile

- Il corpo di `649a554` afferma che i tre file di codice sono stati toccati «solo commenti e
  docstring»: **verificato sul diff**, non sull'intento — il diff è a zero righe di comportamento.
- ~~Se `.claude/agents/*.md` fosse deliberatamente fuori dal corpus~~ — **risolto in questo giro**:
  quelle radici sono dentro (`358263b`), e nulla nel repo le dichiarava esclusi per scelta: era un
  limite ereditato da `RADICI_DOC`, non una decisione. Resta da dire ad alta voce ciò che ora è
  vero: **le istruzioni agite dagli agenti sono corpus**, quindi cambiano il costo di una loro
  modifica — vanno mosse nello stesso commit del codice che descrivono.
- `docs/superpowers/plans/2026-08-14-claude-md-snellito.md:99`: link dentro un esempio di codice.
  Meccanicamente è un candidato della lista chiusa §1, ma correggere l'interno di un esempio
  mostrato in un verbale non è una correzione dimostrabile — segnalato e basta.

## Limite di questo giro

Le cinque affermazioni false corrette/segnate sono venute **tutte** dai commit, **zero** dalle due
chiavi di join: `diff` ha trovato la deriva delle **interfacce** toccate nell'intervallo, e le
interfacce non si erano mosse (solo commenti). Ciò che aveva smesso di essere vero era il
**comportamento** descritto da pagine che quel comportamento non lo nominano con un simbolo.

Restano i limiti strutturali già dichiarati: l'errore di nascita (nessuna di queste cinque: tutte
nate vere e poi rovesciate), e il cambio di corpo senza cambio di firma — che è precisamente la
classe di `e894896`, e che il giro ha pescato **non** per il meccanismo `_REGOLE` (nessun simbolo
prodotto da `frontend/app/budget/page.tsx`) ma perché il messaggio del commit è stato letto per
primo e cercato per argomento.

Fuori dal corpus, non swept e non swept-by-design: `.collaudo/run-02.md:220,470` e
`run-03.md:135,468-470`, che descrivono entrambi la regola dell'aliquota **come era prima di
`073927b`**. Sono spazi di sessione non tracciati (`git ls-files .collaudo` è vuoto), quindi
giustamente esclusi; ma se qualcuno li considerasse materiale di riferimento, la loro frase
sull'aliquota è ora falsa.

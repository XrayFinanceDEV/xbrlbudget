# === Required for production ===
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# === Optional ===
# Dev mode: bypass JWT (set to any string)
# DEV_USER_ID=dev-user-001

# Anthropic API key for fast PDF extraction (Claude Haiku)
# ANTHROPIC_API_KEY=sk-ant-...

# Extra CORS origins (comma-separated)
# ALLOWED_ORIGINS=https://budget.example.com,https://formulafinance.app

# Company limit per user (default: 50)
MAX_COMPANIES_PER_USER=50

# External port (default: 80)
PORT=80

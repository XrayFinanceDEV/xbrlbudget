# Optional NVIDIA acceleration:
# docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d
services:
  mineru:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: ["gpu"]

// Gruppo INDICATORI (v4 pagine 11-18): indicatori del piano, liquidità e
// margini strutturali, redditività e costo del debito, solidità e copertura
// del debito, circolante e ciclo monetario, composizione economica e
// patrimoniale, break-even e margine di sicurezza, diagnostica. Registro
// vuoto lato Python (`dossier_catalog/indicatori.py`): nessuna pagina compare
// ancora, questo file non è importato da `editorial.typ`.
//
// Componenti di questo gruppo che `comuni.typ` non porta ancora, da scrivere
// qui quando servono (v4, non dimostrativi — un solo motore di dati, quello
// del modello v2):
// - dumbbell (pagina 6 «Indicatori dell'infrannuale», rettificato → chiusura;
//   pagina 11 «Indicatori del piano», PFN primo → ultimo anno);
// - barre 100% impieghi/fonti (pagina 16 «Composizione»,
//   `report.structure_series[composition_uses|composition_sources]`);
// - grafico break-even (pagina 17, `report.structure_series.break_even`);
// - panel-grid a due grafici affiancati (pagina 13 «Redditività»: nessuna
//   colonna KPI su quella pagina nella v4, due grafici a piena larghezza).

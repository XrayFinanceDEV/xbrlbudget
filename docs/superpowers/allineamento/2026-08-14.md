# Rapporto di riallineamento — 2026-08-14

**Modo:** `diff` · **Intervallo:** `520fbe8..HEAD` (HEAD = `b66fbf5`, 38 commit) · **Prima
esecuzione dello strumento** — nessuno sweep `--completo` è mai stato lanciato
(`ultimo_completo: null`).

L'intervallo copre il riscatto vision della route C, il primo blocco dello snellimento di
`CLAUDE.md` e la costruzione dello strumento di riallineamento stesso.

## Conteggi

- Simboli mossi estratti: **200**
- Citazioni prodotte: **477**
- Nomi generici esclusi dalla verifica per nome (≥ 40 citazioni, regola `SOGLIA_GENERICO`):
  **2** — `add` (202 citazioni, chiusura locale in `situazione_contabile_parser.py`), `main`
  (77 citazioni, entry point di `scripts/riallinea.py`). Riportati solo per conteggio, non
  aperti singolarmente, come prescritto.
- **Le 477 citazioni superano la soglia di 40 indicata nel brief: lo dichiaro qui invece di
  campionare in silenzio.** Sono state comunque verificate **tutte, integralmente** — nessun
  campionamento — distribuendo il lavoro su tre agenti paralleli più la verifica diretta di
  chi scrive (dettagli in «Taratura», punto 4).

| Verdetto | Citazioni |
|---|---|
| OK | 422 |
| SMENTITO | 14 |
| MORTO | 0 |
| RUMORE (falso positivo del riconoscimento per nome) | 41 |
| **Totale** | **477** |

**Corretto in revisione finale (2026-08-15):** i numeri sopra sostituiscono quelli
della prima stesura (OK 426, SMENTITO 13, RUMORE 38), che si contraddicevano — il
totale RUMORE dichiarato (38) non tornava con la somma della tabella di dettaglio
sotto (36+3=39) — e sottocontavano `__init__` (3 invece di 5 citazioni-rumore reali) e
omettevano una citazione SMENTITO (`riscatto-vision-route-c.md:1492`, vedi Famiglia 1).
Nessuna citazione ha cambiato file o riga; solo la classificazione di alcune è stata
corretta.

## Corretto automaticamente

**Nessuna correzione applicata.** Le 14 citazioni SMENTITO trovate sono tutte descrizioni di
comportamento, firma di funzione o esempi di codice — nessuna rientra nella lista chiusa
dello SKILL (link relativo rotto, percorso di file inesistente, identificatore sparito con
rename inequivocabile via `git log -M --follow`, numero che contraddice una costante
nominata). Vanno tutte in «Da decidere».

## Da decidere

Tutti i seguenti sono in `docs/superpowers/plans/2026-08-14-riscatto-vision-route-c.md`
(piano storico della feature, TDD a task incrementali) salvo l'ultimo. Sono fix successivi al
piano, mai retro-documentati nel piano stesso — non nel codice vivo caricato a ogni sessione.

### Famiglia 1 — `_apply_vision_rescue` ritorna 4 valori, non 3 (7 citazioni)

**Codice attuale** (`importers/pdf_importer.py:391-398`, chiamata reale a `:1283-1286`):
```python
def _apply_vision_rescue(...) -> Tuple[Dict[str, Decimal], Dict[str, Decimal],
                                        List[str], Dict[str, str]]:
```
ritorna `(bs, ce, sezioni_riscattate, motivi)`. Confermato anche dalle 15 chiamate in
`tests/test_vision_rescue.py`, tutte a 4 valori.

| Riga piano | Citato | Problema |
|---|---|---|
| 1387-1388 | `Tuple[Dict, Dict, List[str]]` — "restituisce `(bs, ce, sezioni_riscattate)`" | manca il 4° elemento `motivi` |
| 1419, 1431, 1453, 1474, 1611 | `bs, ce, riscattate = _apply_vision_rescue(...)` (3 variabili) | oggi solleva `ValueError: too many values to unpack` se eseguito |
| 1492 | **corretta in revisione finale (2026-08-15), classificata OK per errore nella prima stesura.** Il blocco `Step 3: Implementare l'helper` che si apre a questa riga (`def _apply_vision_rescue(file_path: str,`) annota `-> Tuple[Dict[str, Decimal], Dict[str, Decimal], List[str]]` alla riga 1498 e ritorna una tupla a 3 valori in quattro punti del proprio corpo (righe 1521, 1526, 1532, 1593) | stesso problema della riga 1387-1388, ma sull'implementazione proposta nel piano, non sulla sua descrizione testuale — identico al caso già segnalato, mancava solo di essere aperto |

**Non riscriviamo il piano** (vedi «Piani e spec datati non si riscrivono» nello SKILL):
è stata aggiunta una nota di superamento in coda a
`docs/superpowers/plans/2026-08-14-riscatto-vision-route-c.md` che rimanda alla firma e
al comportamento reali di `_apply_vision_rescue` in `importers/pdf_importer.py`.

### Famiglia 2 — `mastro_level_rows` ha un parametro e un algoritmo diversi (3 citazioni)

**Codice attuale** (`importers/vision_rescue.py:147-149`):
```python
def mastro_level_rows(rows: Sequence[VisionRow],
                      totals: Optional[Dict[str, Optional[Decimal]]] = None
                      ) -> Tuple[VisionRow, ...]:
```
Non tiene più "il livello con meno cifre": prova le partizioni per lunghezza di codice e
tiene la prima che riconcilia ai totali di colonna letti, ricadendo sul minimo solo se
nessuna riconcilia (fix per un bug reale su `budget_624`, documentato nel docstring).

| Riga piano | Citato | Problema |
|---|---|---|
| 731 | `mastro_level_rows(rows: Sequence[VisionRow]) -> Tuple[VisionRow, ...]` | manca il parametro `totals` |
| 991 | firma + corpo/docstring embedded, versione "minimo cifre" | algoritmo superato |
| 1099 | `rows=mastro_level_rows(rows),` | il sito reale passa anche `totals` |

**Non riscriviamo il piano** — stessa motivazione della Famiglia 1: è un verbale
datato, non documentazione viva. La nota di superamento aggiunta in coda al file
copre anche `mastro_level_rows` (`importers/vision_rescue.py:147-149` per la firma e
l'algoritmo attuali).

### Famiglia 3 — `accept_rescue`: firma più ampia e più di "tre condizioni" (3 citazioni)

**Codice attuale** (`importers/vision_rescue.py:398-405`): 8 parametri, non 6 (`residual_measured`,
`rebuilt_passivo` aggiunti). Oltre alle tre condizioni del piano il corpo applica: guardia
`section == sec.section`; una riconciliazione separata sul passivo quando `rebuilt_passivo` è
fornito (`:441-452`, commit `9ffdb14`); un veto se il riscatto rompe l'identità utile
CE = `sp13` (`:454-457`). `docs/import/REGOLE-IMPORT-02-ESTRAZIONE.md` (verificato in questo
stesso giro, vedi sotto) descrive correttamente le 5 condizioni attuali — è solo il piano a
essere rimasto alla versione a 3.

| Riga piano | Citato | Problema |
|---|---|---|
| 1140 | firma a 6 parametri | oggi sono 8 |
| 1326 | blocco di codice/docstring "Tre condizioni, tutte necessarie" | versione pre-fix |
| 1746 | "tiene il riscatto solo se tutte e tre: [...]" | non menziona il controllo sul passivo né il veto sull'identità utile |

**Nota di contesto:** questo paragrafo del piano (Task 8) era stato copiato in `CLAUDE.md` dal
commit `5062657`, ma **rimosso** da `68a79c4` nell'ambito dello snellimento di `CLAUDE.md` in
corso — oggi non c'è un'affermazione falsa viva nel file caricato a ogni sessione, il rischio
resta confinato al documento di piano.

**Non riscriviamo il piano** — stessa motivazione della Famiglia 1. La versione a 8
parametri e 5 condizioni (riconciliazione colonna sinistra; riconciliazione colonna
destra quando misurabile; estrazione non vuota; identità utile CE=sp13 non spenta;
quadratura strettamente migliore) è già descritta correttamente nel documento vivo
`docs/import/REGOLE-IMPORT-02-ESTRAZIONE.md`; la nota di superamento in coda al piano
rimanda lì.

### `docs/superpowers/specs/2026-07-06-pratica-workflow-reorganization-design.md:173`

**Dice:** che il promote endpoint "crea lo scenario draft collegato" quando la catena
`source_scenario_id` non ne ha uno, e che la risposta guadagna una chiave `budget_scenario`.

**Codice attuale:** `backend/app/services/promote_service.py:16-155` copia solo BS/IS da
`ForecastYear` a `FinancialYear` — nessuna creazione di scenario, nessuna lettura/scrittura di
`source_scenario_id` (`git grep "source_scenario_id\s*="` → zero assegnazioni fuori da
schema/model). La risposta dell'endpoint resta il dict del service (`success,
financial_year_id, year, company_id, message`), mai `budget_scenario` — coerente con quanto
`CLAUDE.md` descrive oggi ("Promote Infrannuale → Budget Workflow": passo manuale separato).

**Proposta non applicata:** è uno spec del 2026-07-06 mai realizzato così com'era progettato;
non riscriverei la frase come se il codice fosse sbagliato — aggiungerei una nota "❌ Non
implementato: il promote endpoint non crea scenari collegati" sotto il punto elenco.

## Memoria — riferimenti morti

**Nessuno.** Le uniche 3 citazioni pescate nella memoria
(`~/.claude/projects/-home-peter-DEV-budget/memory/commit-directly-to-main.md`, righe 3, 12,
14) sono tutte rumore — falsi positivi su "create" come verbo inglese comune ("do not create
feature branches"), non riferite al simbolo di codice `create`. Nessun riferimento morto
trovato; il file di memoria non è stato toccato.

## Non verificabile

Nessuna citazione di questo giro è finita in questa categoria: tutte e 477 sono state aperte
contro il codice attuale con esito determinabile.

## Rumore — sintesi (41 citazioni, quasi tutte due simboli)

**Corretto in revisione finale (2026-08-15):** la riga `__init__` sotto era 3 nella
prima stesura; il riconteggio diretto delle citazioni (`git grep`/rilettura una per
una) ne trova **5**, non 3 — la tabella e il totale di sezione sono stati corretti di
conseguenza (38 → 41 = 36 + 5).

| Simbolo | Citazioni rumore | Causa |
|---|---|---|
| `create` | 36 | Verbo inglese comune ("create a scenario", "create in appropriate router", colonna di stato "(create)" in una tabella, comando CLI Netlify). Il simbolo reale che l'estrazione ha agganciato è `_FakeMessages.create` (`tests/test_vision_rescue.py:231`, mock di `client.messages.create` dell'SDK Anthropic) — **38 citazioni totali per `create`, di cui solo 2 genuinamente riferite a quel metodo mock** (righe 760/1071 del piano riscatto-vision, contate OK). |
| `__init__` | 5 | Percorsi di file (`tests/__init__.py` ×2, `scripts/__init__.py` ×2, citati come nome-file) o costruttore di una classe non correlata (`MinerUClient.__init__` ×1), mai i costruttori delle classi-finte `_FakeBlock`/`_FakeResponse`/`_FakeMessages`/`_FakeClient` a cui l'estrazione li ha agganciati (quei 4 sono OK, non rumore). |

`create` ha 38 citazioni totali nell'estrazione grezza — **appena sotto** `SOGLIA_GENERICO = 40`
— quindi non è stato instradato verso `generici` e ha dovuto essere verificato citazione per
citazione. Vedi «Taratura» punto 1.

## Limite dichiarato (diff mode)

Un controllo guidato dal diff trova la **deriva**, non l'**errore di nascita**: una frase
sbagliata su codice mai toccato in questo intervallo non viene mai segnalata. Vedi «Taratura»
punto 3 per una prova diretta di questo limite su questo stesso intervallo.

---

## Taratura

**1. Quanti simboli ha estratto e quanti erano rumore?**
200 simboli estratti. Di questi, il rumore effettivo si concentra su **un solo nome**,
`create` (metodo mock in un file di test, sintatticamente indistinguibile dal verbo inglese
comune), più in misura minore `__init__` (costruttori Python, altrettanto generico). Tutti
gli altri ~198 simboli sono nomi di dominio specifici (`VisionRow`, `classify_page_section`,
`accept_rescue`, `mastro_level_rows`, `Simbolo`, `Citazione`, ecc.) e non hanno prodotto un
solo falso positivo. Il problema non è "molti simboli rumorosi", è che il filtro generico
(soglia fissa a 40 citazioni) ha lasciato passare un nome che si comporta esattamente come i
49 nomi già esclusi, solo con 2 citazioni di margine (38 contro 40).

**2. Le citazioni trovate erano pertinenti, o il filtro per nome ha pescato per lo più
occorrenze irrilevanti?** Per lo più pertinenti: 422/477 (88,5%) erano affermazioni verificabili
e confermate dal codice, 14/477 (2,9%) erano disallineamenti reali, e solo 41/477 (8,6%) erano
rumore puro — e quell'8,6% è quasi interamente riconducibile a un singolo nome. Il filtro per
nome, sui simboli specifici che costituiscono la stragrande maggioranza dell'estrazione, ha
funzionato bene: ogni citazione di `VisionRow`, `accept_rescue`, `section_pages`,
`mastro_level_rows`, ecc. era una vera affermazione su quel simbolo, mai un'omonimia casuale.

**3. Il filtro per nome ha perso qualcosa che si sapeva già essere disallineato?** **Sì**,
esattamente il caso indicato nel brief. `_validate_equity` (`importers/pdf_extractor_llm.py:3693`)
è codice morto — nessuna riga lo chiama, verificato con `git grep -n -w _validate_equity` (3
occorrenze: 1 definizione, 2 commenti, 0 chiamate) — e fino a stamattina due documenti lo
davano per attivo; il commit `73b01c8`, **dentro questo stesso intervallo**, ha già corretto
quei due documenti. Lo strumento **non ha estratto `_validate_equity` come simbolo mosso**,
perché la sua definizione (riga 3693) non è stata toccata dal diff — il file
`pdf_extractor_llm.py` ha ricevuto solo 23 righe aggiunte altrove (due nuove ancore per il
CE), quindi le citazioni corrette stamattina non compaiono affatto in questo report: sono
finite in "Corretto automaticamente" da un umano prima ancora che lo strumento potesse
vederle. Non è un bug dello strumento — è esattamente il limite che lo SKILL dichiara a parole
("un controllo guidato dal diff trova la deriva, non l'errore di nascita") — ma qui è una
prova concreta, non solo teorica: su questo intervallo reale il modo `diff` **non avrebbe mai
potuto** trovare da solo il disallineamento di `_validate_equity`, perché quel disallineamento
non nasce da un simbolo mosso in questo diff. Solo uno sweep `--completo` (mai eseguito,
`ultimo_completo: null`) lo intercetterebbe.

**4. Quanto è durata l'esecuzione, e quante affermazioni ho verificato davvero?**
L'estrazione meccanica (`scripts/riallinea.py`) è quasi istantanea (pochi secondi). La verifica
umana/agente ha richiesto in totale circa 45-50 minuti di lavoro effettivo, distribuiti su tre
sub-agenti paralleli più il lavoro diretto di chi scrive:
- Gruppo B (111 citazioni, piano+spec dello strumento stesso): ~4,3 min, 111/111 verificate.
- Gruppo D (35 citazioni, 14 documenti sparsi): ~8,2 min, 35/35 verificate individualmente.
- Gruppo A (307 citazioni, piano+spec riscatto vision, il grosso): ~20,4 min, 307/307
  verificate (con doppia verifica indipendente su un sottoinsieme, per via del fork interno
  dell'agente).
- Diretto (24 citazioni: CLAUDE.md, `REGOLE-IMPORT-02-ESTRAZIONE.md`, l'inventario, la
  memoria) + verifica mirata di `_validate_equity`: ~15 min.

**Tutte le 477 citazioni sono state aperte e confrontate col codice reale — nessun
campionamento silenzioso**, come richiesto esplicitamente quando il numero supera 40.

**5. Lo strumento serve?**
Sì, ma con una calibrazione precisa di che cosa fa bene e che cosa no. Su questo intervallo
reale e sostanzioso (38 commit, 7.881 righe aggiunte) ha trasformato 477 citazioni sparse su
22 documenti in **14 punti precisi, verificati e pronti per la revisione umana**, con riga di
codice a fianco di ogni affermazione contestata — un risparmio reale rispetto a rileggere a
mano quei 22 documenti. Il tasso di rumore (8,6%, concentrato su due nomi) è basso e
correggibile con un intervento mirato (vedi sotto). Non ha prodotto **nessun falso MORTO** —
zero simboli dichiarati inesistenti quando in realtà esistevano con un nome diverso o come
alias, il tipo di errore più costoso da correggere alla cieca.

Detto questo, due limiti reali, non teorici:
- **In questa esecuzione lo strumento non ha corretto nulla da solo** (0/14 disallineamenti
  reali rientravano nella lista chiusa) — oggi è, in pratica, un **rilevatore di candidati**
  per la revisione umana, non un correttore automatico. È una scelta deliberata dello SKILL
  ("una correzione sbagliata è peggio del disallineamento"), non un difetto, ma va dichiarata
  per calibrare le aspettative: chi lancia `/riallinea` aspettandosi che risolva da solo la
  maggior parte dei problemi resterà deluso — il suo valore è nell'aver ridotto il problema a
  14 righe leggibili in dieci minuti, non nell'averlo chiuso.
- **La cecità sul codice non toccato dal diff è reale e misurata qui**, non solo dichiarata:
  vedi punto 3. Il modo `diff` da solo non basta a tenere la documentazione allineata nel
  tempo; serve affiancarlo, prima o poi, a uno sweep `--completo` (mai fatto finora).

Correzione raccomandata per la prossima iterazione dello strumento meccanico (fuori dal
perimetro di questo task, che non tocca `scripts/riallinea.py`): abbassare `SOGLIA_GENERICO` o
aggiungere una lista nera di nomi strutturalmente generici (`create`, `__init__`, `__repr__`
e altri dunder Python) indipendentemente dal conteggio di soglia — `create` a 38 citazioni ha
richiesto una verifica individuale di 36 falsi positivi per trovarne 2 veri.

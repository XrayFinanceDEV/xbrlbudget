// Gruppo DATI (v4 pagine 3-6, solo workflow infrannuale): bilancio
// infrannuale e fonti, rettifiche apportate, dall'infrannuale alla chiusura,
// indicatori dell'infrannuale. `dossier_catalog/dati.py` ha registro vuoto in
// questa fase: nessuna pagina compare, quindi questo file non è ancora
// importato da `editorial.typ`. Quando `dati.py` produrrà pagine, i loro
// blocchi generici (KPI, grafico, tabella) si rendono già con `comuni.typ`;
// qui va solo ciò che è specifico del gruppo (per esempio il dumbbell
// «rettificato → chiusura» della pagina 6, o il grafico prima/dopo della
// pagina 4 — nessuno dei due esiste ancora in `comuni.typ`, vedi la nota in
// quel file).
